require('dotenv').config({ path: '.env.local' });
// Because api.js uses ES modules and we are in Node, it's easier to test by mocking or just seeing the frontend. But we can write a quick mjs script.
